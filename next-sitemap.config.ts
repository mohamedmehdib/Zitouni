import { IConfig } from 'next-sitemap';

const config: IConfig = {
  siteUrl: 'https://zitouni-pro-talent.tn',
  generateRobotsTxt: true, // This auto-creates robots.txt too
  sitemapSize: 7000,
};

export default config;
